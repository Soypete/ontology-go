version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - govet
    - ineffassign
    - unused
    - misspell
    - staticcheck
  exclusions:
    rules:
      - path: skosast/builder.go
        linters:
          - unused
        text: "termToString"
      - linters:
          - staticcheck
        text: "QF1003"
      - linters:
          - staticcheck
        text: "QF1001"
      - linters:
          - staticcheck
        text: "QF1008"
      - linters:
          - staticcheck
        text: "SA6005"
      - linters:
          - errcheck
        text: "Error return value"
      - linters:
          - unused
        text: "owlClass"
      - linters:
          - unused
        text: "func termToString"

issues:
  max-issues-per-linter: 0
  max-same-issues: 0